import { ethers } from "hardhat";
// Constants
export const BATCH_DESCRIPTION = "Batch test description";
export const EVENT_DOCUMENT_HASH = ethers.utils.formatBytes32String(
  "Event document test hash"
);
